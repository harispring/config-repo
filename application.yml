server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/your-org/config-repo.git
          # If private repo, add username/password or use SSH
          #username: your-username
          #password: your-password
          # branch: main      # Optionally specify branch
